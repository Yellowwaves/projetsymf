<form method="post" action="{{ path('app_seismes_delete', {'id': seisme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seisme.id) }}">
    <button class="btn">Delete</button>
</form>
